# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which
treats the code as read-only ground truth and edits only documentation so it reflects the
repo's actual current state. No code was changed and no builds or tests were run. It exists
to give any reviewer a full audit trail of what was checked, what was changed, and what
requires a human's eye.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

All seven chapters are present and implemented:
- `src/chapters/the-test.ts`, `the-signal.ts`, `the-forecast.ts`, `the-update.ts`,
  `the-market.ts`, `the-evidence.ts`, `the-convergence.ts` — all exist with full configs.
- Corresponding content components (`src/components/chapter/content/The*Content.tsx`) exist
  for all seven, each marked `'use client'` as required for interactive viz.
- All seven chapter slugs are wired into `src/chapters/index.ts` and loaded via `dynamic()`
  in `src/app/chapter/[slug]/page.tsx`.

README intro, CLAUDE.md purpose, and AGENTS.md project goal all accurately describe the
project as a seven-chapter interactive Bayesian reasoning essay. No changes made.

---

### 2. Current state

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

Docs claim "v1.0.0 — Complete, All 7 chapters implemented and playable." Code confirms:
- `package.json:3` — `"version": "1.0.0"`
- All 7 chapter config files, visualization components, and content components exist.
- 11 Vitest test files present across `src/chapters/` and `src/lib/`.
- 1 Playwright E2E smoke spec at `e2e/smoke.spec.ts` (3 tests: landing, chapter load, nav).
- `src/lib/math.ts` includes inline `console.assert` checks as the convention requires.

**Implementation gap (persistent from prior pass):** `src/app/chapter/[slug]/page.tsx:51`
references `/og/${chapter.slug}.png` for OpenGraph images, but `public/og/` does not exist —
no OG image files were found. Docs note this as architectural intent; images are missing.
See "Contradictions for Manual Review."

---

### 3. Stack

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`package.json` confirms: `next@^14.2.0`, `react@^18.3.0`, `typescript@^5.5.0`, `d3@^7.9.0`,
`katex@^0.16.0`, `framer-motion@^11.0.0`, `tailwindcss@^3.4.0`.

`next.config.mjs:3` confirms `output: "export"` (full static site).

`@vercel/analytics@^2.0.1` is in `package.json` dependencies and is imported and rendered in
`src/app/layout.tsx:1,36`. Docs reflect this: CLAUDE.md Gotchas and AGENTS.md Core Rules
both correctly describe the analytics posture as of 2026-05-11.

`src/components/viz/DotGrid.tsx` uses `HTMLCanvasElement` with `canvas.getContext("2d")` —
consistent with the documented "Canvas (not SVG) for the dot grid" gotcha.

---

### 4. How to run

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README (`npm run dev`, `npm run build`, `npm run typecheck`) — all defined in `package.json:6-13`.
The previously drifted `npm run build && npm run start` was fixed in commit `63c97ab`.

CLAUDE.md Build/Run section lists `pnpm dev`, `pnpm build`, `pnpm lint` — `pnpm-lock.yaml`
is present, confirming pnpm as the primary package manager for the dev environment.

AGENTS.md canonical verifier (`npm ci`, `npm test`, `npm run typecheck`, `npm run build`)
matches `.codex/verify.commands` exactly and aligns with `.github/workflows/ci.yml` steps.

No changes made.

---

### 5. Known risks (doc contradictions in editable files)

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

All previously drifted items in editable files were fixed in the prior reconciliation pass
(`63c97ab`) and subsequent lean commit (`07c06ae`):
- CLAUDE.md now has a "Gotchas" section (replacing the old "Do NOT" list) with the correct
  analytics posture: `@vercel/analytics` active, Sentry not enabled.
- AGENTS.md Core Rules correctly restricts tracking to "beyond the @vercel/analytics
  integration already in layout.tsx."
- README Quick Start no longer mentions `npm run start` (incompatible with static export mode).

No new contradictions found in editable doc files. No changes made.

---

### 6. Next move

**Status:** `unverifiable`
**Evidence:** `unverifiable-because-forward-looking`

CLAUDE.md Portfolio Context says "Use this context plus the README and supporting docs to
resume the next active task." This is an operational directive, not a verifiable state claim.
Left unchanged.

---

## Contradictions for Manual Review

The following are in files outside the editable set (`IMPLEMENTATION-ROADMAP.md`,
`CHANGELOG.md`, `docs/PORTFOLIO-DISPOSITION.md`). No edits were made; human action required.

### `IMPLEMENTATION-ROADMAP.md` — Analytics hard constraints (Section 2, line ~57 and Section 5, line ~560)
**What it says:** "No analytics (v1)" in Review Gate Hard Constraints; "No tracking, no analytics (v1)" in the Security section; Analytics row in Locked Decisions table: "None (v1) | Simplicity; add Vercel Analytics in v2 with one line."
**What the code shows:** `@vercel/analytics` is installed (`package.json:15`) and active (`src/app/layout.tsx:1,36`). Portfolio-wide analytics policy was applied 2026-05-11, superseding the v1 constraint.
**One-line fix:** Strike through all three occurrences and append: `~~No analytics (v1)~~ — superseded 2026-05-11; @vercel/analytics active.`

### `IMPLEMENTATION-ROADMAP.md` — Testing strategy (Section 6, line ~573)
**What it says:** "Write `src/lib/math.test.ts` using Node's built-in `assert` module … Run with `node --experimental-strip-types src/lib/math.test.ts` in CI."
**What the code shows:** `src/lib/math.test.ts:1` uses Vitest (`import { describe, expect, it } from "vitest"`). The `package.json` test script is `vitest run`. The Node-assert approach was never implemented.
**One-line fix:** Replace the Node/assert testing description with: "Tests use Vitest (`npm test`); co-located `*.test.ts` files cover math functions and chapter compute functions."

### `CHANGELOG.md` — Version mismatch
**What it says:** Only `## [Unreleased]` with "Initial release."
**What `package.json` shows:** `"version": "1.0.0"`. All 7 chapters shipped and playable.
**One-line fix:** Add a `## [1.0.0]` section with the shipped feature list; move "Initial release" content there.

### `docs/PORTFOLIO-DISPOSITION.md` — Stale tip sha
**What it says:** "Tip: `75c0ea8` feat: add Playwright E2E smoke tests" and "Last known reference → `origin/main` tip: `75c0ea8`."
**What git shows:** Current HEAD is `07c06aef1994ac815b3d75dc6ab061fc6d71a4c1` (7 commits ahead, all documentation and analytics). The document predates the analytics integration and subsequent CLAUDE.md reconciliation passes.
**One-line fix:** Update tip sha and add a note that commits `c599548`–`07c06ae` added analytics integration and doc reconciliation passes.

### Missing OG images (chapter page metadata — persistent)
**What docs say:** CLAUDE.md Architecture — "OG images: static PNGs in `/public/og/`"; IMPLEMENTATION-ROADMAP Phase 4 task — create 1200×630 PNGs per chapter.
**What the code shows:** `src/app/chapter/[slug]/page.tsx:51` references `/og/${chapter.slug}.png`, but `public/og/` does not exist. Seven OG images are missing; social shares will use the fallback metadata description only.
**Action required:** Create seven 1200×630 PNG files at `public/og/the-test.png`, `the-signal.png`, `the-forecast.png`, `the-update.png`, `the-market.png`, `the-evidence.png`, `the-convergence.png`.

---

## Footer

**Timestamp:** 2026-06-02 19:50:44 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD reconciled against:** `07c06aef1994ac815b3d75dc6ab061fc6d71a4c1`
